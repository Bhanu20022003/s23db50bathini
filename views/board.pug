extends layout

block content
  h1= title
  p rows: #{query.rows} , columns: #{query.cols}
  - let rows = Number(query.rows)
  - let col = Number(query.cols)
  - if (!Number.isInteger(rows) || !Number.isInteger(col))
    P rows and columns values must be of type Integers
  - else if(rows < 1 || rows > 10)
    p rows value must be in the range of 1 to 10
  - else if(col < 1 || col > 10)
    p cols value must be in the range of 1 to 10
  - else
    table#outer
      - for(let i = 0; i < rows; i++)
        tr#middle
          - for(let j = 0; j < col; j++)
            - let val = 0;
              - if (i === j)
                - val = 1;
              - else if (i === j + 1)
                - val = 2;
              - else if (i === j - 1)
                - val = i;
              td= val